Read me:

For the credentials of the database:
host: localhost
uname: root
password: no password
database: atlanta-test


To add data: Enter comment on the comment form then click the + button.
To update: Enter comment on the comment form then click Update button on the
		row that needs to be updated.
To delete: Just click the row for deletion then confirm OK on the confirmation.